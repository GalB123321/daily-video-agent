# Python dependencies for the daily video agent.
# Note: ffmpeg is a system dependency installed via Homebrew (brew install ffmpeg),
# it is not a pip package and is invoked through subprocess.

auto-editor
faster-whisper
PyYAML
# boto3 is used only by the render worker (worker.py) to read and write clips
# and outputs on the R2 bucket over the S3 API. The daily pipeline does not need it.
boto3
